import 'package:flutter/material.dart';

/// Shared spacing and width constants for the warning screen.
class WarningLayout {
  static const double maxContentWidth = 640;
  static const double compactWidth = 330;
  static const EdgeInsets pagePadding = EdgeInsets.fromLTRB(16, 18, 16, 28);
  static const EdgeInsets cardPadding = EdgeInsets.all(18);
}
